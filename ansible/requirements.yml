# Install a role from GitHub
# To install:
#   ansible-galaxy install -r requirements.yml
#

# main dokku library
- name: dokku_bot.ansible_dokku
  src: https://github.com/dokku/ansible-dokku
  version: v2020.3.24

# unattended upgrades
- name: jnv.unattended-upgrades
  src: https://github.com/jnv/ansible-role-unattended-upgrades
  version: master

# chrom install for client machine
- name: wtanaka.chrome
  src: https://github.com/wtanaka/ansible-role-chrome
  version: master
